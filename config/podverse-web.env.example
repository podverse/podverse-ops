NODE_ENV=production
PROTOCOL=http
PORT=8765

API_PROTOCOL=http
API_PORT=1234
API_PATH=api
API_VERSION=v1

COOKIE_DOMAIN=stage.podverse.fm
COOKIE_PATH=/

PAYPAL_ENV=sandbox
PAYPAL_CLIENT_ID_PRODUCTION=
PAYPAL_CLIENT_ID_SANDBOX=

GOOGLE_ANALYTICS_TRACKING_ID=

# For back-end use docker container namespaces for domains
DOMAIN=podverse_web:8765
API_DOMAIN=podverse_api:1234

# For front-end use public domain names
PUBLIC_DOMAIN=stage.podverse.fm
PUBLIC_API_DOMAIN=api.stage.podverse.fm

# PROD/STAGE only
# domain for nginx to use
VIRTUAL_HOST=stage.podverse.fm
