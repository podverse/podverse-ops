pipeline {
    agent {
        label 'pv-alpha-aux'
    }

    stages {
        stage('Run Script') {
            steps {
                sh """#!/usr/bin/env bash
                    set -e
                    pushd /opt/podverse-ops
                    make alpha_db_down
                    popd
                """
            }
        }
    }
}